---
jupyter:
  jupytext:
    text_representation:
      extension: .Rmd
      format_name: rmarkdown
      format_version: '1.2'
      jupytext_version: 1.19.1
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

```{python}
# %load_ext autoreload
# %autoreload 2
```

```{python}
from pathlib import Path
```

```{python}
project_path = Path.home()/"Projects/LFT"
```

```{python}
run_path = project_path/"EA_runs"
```

```{python}
data_path = run_path/"data"
```

```{python}
import numpy as np
import torch
import matplotlib.pyplot as plt
import yaml
import scipy
from scipy.stats import norm
```

```{python}
import dneumc
```

```{python}
import test_utils as tu
```

```{python}
from dneumc.ising.ea import Energy
```

```{python}
from neumc.utils.stats_utils import bootstrap, ac_and_tau_int, torch_bootstrap
```

```{python}
from rich.table import Table
```

```{python}
def hist_int(a,*,bin_width,**kwargs):
    a_min = a.min()
    a_max = a.max()
    bins = int(round(a_max-a_min)/bin_width)+1
    return plt.hist(a,bins=bins, range=(a_min-bin_width/2, a_max+bin_width/2), **kwargs )
```

```{python}
Lx = 64
Ly = 64
```

```{python}
J_t = np.loadtxt(run_path/"J_64x64_new_05.txt")
```

```{python}
tag = "run_01_J5"
```

```{python}
name = f"{Lx:03d}x{Ly:03d}_{tag}"
```

```{python}
options = tu.read_options(data_path, name)    
n_betas=len(options['beta'])
print(options['beta'])
```

```{python}
J = np.loadtxt(data_path/f"j_{name}.txt")
```

```{python}
np.allclose(J_t,J) 
```

```{python}
em = tu.read_em_files(data_path, name, options['beta'])
```

```{python}
em.shape
```

```{python}
d=1
taus1=[]
taus2=[]
for i in range(n_betas):
  tau, ac = ac_and_tau_int(em[i][::d,1].numpy());
  taus1.append(tau)
  tau, ac = ac_and_tau_int(em[i][::d,3].numpy());
  taus2.append(tau)
  
```

```{python}
plt.scatter(options['beta'], taus1); 
plt.scatter(options['beta'], taus2); 
```

```{python}
for i in range(n_betas):
  hist_int(em[i][:,0].numpy(),bin_width=4, histtype='step', label=f"{options["beta"][i]:.2f}")
#plt.legend();  
```

```{python}
qs = [em[i ][:,4].mean() for i in range(n_betas)]
```

```{python}
plt.scatter(options['beta'], qs);
```

```{python}
n_replicas = 2
```

```{python}
cfgs = torch.from_numpy(np.fromfile(f"{ea_dir}/cfg_{name}_b{i:02d}.bin", dtype="int8").reshape(-1,n_replicas,Lx,Ly))
```

```{python}
cfgs.shape
```

```{python}
(cfgs[:,0,0:2,0:4].reshape(-1,8).to(int)+1)//2
```

```{python}
max([1,3,2,-1])
```

```{python}
tag='b1900'
```

```{python}
name = f"{Lx:03d}x{Ly:03d}_{tag}"
```

```{python}
options = tu.read_options(data_path, name)    
n_betas=len(options['beta'])
print(options['beta'])
```

```{python}
em = tu.read_em_files(data_path, name, options['beta'])
```

```{python}
em.shape
```

```{python}
tau, _, = ac_and_tau_int(em[0,:,1].numpy()); 
tau
```

```{python}

```
